import 'package:flutter/material.dart';

// Primary colors
const Color customIndigoColor = Color.fromRGBO(65, 71, 220, 1);

// Grey colors
final Color customGreyColor600 = Colors.grey[600]!;
final Color customGreyColor400 = Colors.grey[400]!;

// Base colors
const Color white = Colors.white;
const Color black = Colors.black;
const Color transparent = Colors.transparent;

// Error colors
const Color errorColor = Colors.red;

// Surface colors
const Color surfaceColor = white;
const Color surfaceContainerColor = white;

// Text colors
const Color primaryTextColor = black;
const Color secondaryTextColor = Color.fromRGBO(100, 100, 100, 1);

// Icon colors
const Color iconColor = black;
